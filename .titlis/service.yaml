apiVersion: titlis.io/v1
kind: Service
metadata:
  name: titlis-scoreops
spec:
  product: titlis
  team: platform
  tier: tier-1
  description: Motor de scoring — avalia regras e calcula scores (Go/chi).
  workloads:
    - titlis-scoreops
